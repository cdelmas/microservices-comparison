TODO list
- common
	- add a README
	- add a test client

- restlet
	
- dropwizard

- spring boot
	- add https server
	- add dependency injection
	- add rest resources
	- add Location header
	- add resource call
	- add Facebook auth
	- add links in representations
	- add links in list representation

- vertx
	- add resource call (does not work -> event loop); should inspect more precisely the framework, it seems that it can be very cool for microservices as verticles !
	- add Facebook auth : manual, so should be ok... Not tested

- sparkjava + unirest
	- add https server
	- add dependency injection
	- add rest resources
	- add Location header
	- add resource call
	- add Facebook auth
	- add links in representations
	- add links in list representation

